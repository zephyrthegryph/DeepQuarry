// DQEdit — All defines that were previously here moved into code/__defines/preferences.dm
// so they're visible to the prefs subsystem in includes-order. This file is kept as a
// placeholder + reminder of where to look. Don't add new defines here; add them next to
// the existing PREFERENCE_CHARACTER / PREF_WIDGET_* defines in code/__defines/preferences.dm.
