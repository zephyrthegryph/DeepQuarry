# Type-level list vars that allocate a list for every instance, kept on purpose.
# tools/ci/instance_list_lint.py reads this file; format `/type/path/var # reason`.
# New entries need a real reason (see AGENTS.md 3a): prefer a static var or getter
# for constant tables, a lazy list for usually-empty data, or a shared
# copy-on-write list for rarely-written data. Remove an entry when you convert it.
# The "baseline" section is the audit backlog (doc/rewrite/memory_lists_audit.md),
# not an endorsement: shrink it, don't grow it.

# Kept: class (d), or already shared at runtime.
/datum/hud_data/gear # kept: nested table shared per type in New(); the eager copy is dropped
/datum/internal_wiki/page/data # d: every page fills its data table
/datum/pipe_network/gases # d: pipe network membership; atmos agents own this area
/datum/pipe_network/leaks # atmos area (M1a/S1): listed in memory_lists_audit.md, not edited here
/datum/pipe_network/line_members # d: pipe network membership; atmos agents own this area
/datum/pipe_network/normal_members # d: pipe network membership; atmos agents own this area
/datum/radio_frequency/devices # d: frequencies exist because devices joined them
/datum/unarmed_attack/attack_noun # kept: interned with string_list() in New()
/datum/unarmed_attack/attack_verb # kept: interned with string_list() in New()
/obj/item/armor # kept: interned per contents in Initialize(); own_armor() before writes
/turf/simulated/wall/tgmc/blend_log # rare: tgmc walls are unmapped; debug log

# Medical session areas (not edited here).
/datum/robolimb/species_alternates # medical session owns code/modules/organs/robolimbs.dm; see memory_lists_audit.md
/datum/robolimb/species_cannot_use # medical session owns code/modules/organs/robolimbs.dm; see memory_lists_audit.md
/datum/surgery_status/in_progress # medical session owns code/modules/surgery/surgery.dm; see memory_lists_audit.md
/datum/surgery_step/excludes_steps # medical session owns code/modules/surgery/surgery.dm; see memory_lists_audit.md
/obj/item/organ/assists_languages # medical session owns code/modules/organs/organ.dm; see memory_lists_audit.md
/obj/item/organ/autopsy_data # medical session owns code/modules/organs/organ.dm; see memory_lists_audit.md
/obj/item/organ/external/children # medical session owns code/modules/organs/organ_external.dm; see memory_lists_audit.md
/obj/item/organ/external/implants # medical session owns code/modules/organs/organ_external.dm; see memory_lists_audit.md
/obj/item/organ/external/internal_organs # medical session owns code/modules/organs/organ_external.dm; see memory_lists_audit.md
/obj/item/organ/external/markings # medical session owns code/modules/organs/organ_external.dm; see memory_lists_audit.md
/obj/item/organ/internal/augment/armmounted/shoulder/multiple/integrated_tools # medical session owns code/modules/organs/internal/augment/armmounted.dm; see memory_lists_audit.md
/obj/item/organ/internal/augment/armmounted/shoulder/multiple/synth_types # medical session owns code/modules/organs/internal/augment/armmounted.dm; see memory_lists_audit.md
/obj/item/organ/internal/eyes/eye_colour # medical session owns code/modules/organs/internal/eyes.dm; see memory_lists_audit.md
/obj/item/organ/internal/lungs/replicant/mending/repair_list # medical session owns code/modules/organs/subtypes/replicant.dm; see memory_lists_audit.md
/obj/item/organ/internal/nano/refactory/materials # medical session owns code/modules/organs/subtypes/nano.dm; see memory_lists_audit.md
/obj/item/organ/target_parent_classes # medical session owns code/modules/organs/organ.dm; see memory_lists_audit.md
/obj/item/organ/trace_chemicals # medical session owns code/modules/organs/organ.dm; see memory_lists_audit.md
/obj/item/organ/will_assist_languages # medical session owns code/modules/organs/organ.dm; see memory_lists_audit.md
/obj/item/rig_module/protean/armor/armor_settings # medical session owns code/modules/clothing/spacesuits/rig/modules/protean.dm; see memory_lists_audit.md

# Mobs: few at boot.
/mob/active_genes # mob: 15 mobs at boot; per-instance state, see audit
/mob/following_mobs # mob: 15 mobs at boot; per-instance state, see audit
/mob/grabbed_by # mob: 15 mobs at boot; per-instance state, see audit
/mob/language_keys # mob: 15 mobs at boot; per-instance state, see audit
/mob/languages # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bad_external_organs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/botcard_access # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/cleanbot/target_types # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/ignore_list # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/ignore_past # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/patrol_path # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/req_access # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/req_one_access # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/secbot/preparing_arrest_sounds # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/bot/target_path # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/antibodies # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/all_underwear # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/all_underwear_metadata # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/flavor_texts # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/genetic_side_effects # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/hide_underwear # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/overlays_standing # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/human/teleporters # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/carbon/worn_clothing # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/custom_cold # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/custom_heat # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/dominated_brain/prey_langs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/internal_organs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/internal_organs_by_name # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/modifiers # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/organs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/organs_by_name # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/additional_law_channels # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/ai/all_eyes # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/ai/connected_robots # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/ai/multicam_screens # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/ai/network # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/pai/software # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/queued_alarms # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/robot/req_access # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/robot/robotdecal_on # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/robot/sprite_extra_customization # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/robot/vore_light_states # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/silicon_subsystems # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/speech_synthesizer_langs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/silicon/stating_laws # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/animal/synx/marking_styles # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/animal/synx/overlay_colors # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/animal/synx/speak # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/animal/synx/voices # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/armor # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/attacktext # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/construct/construct_spells # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/fleshtaker/base_values # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/friendly # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/friends # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/loot_list # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/mechanical/ward/monitor/seen_mobs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/myid_access # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/slime/xenobio/slime_mutation # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/speech_sounds # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/aggressive/macrophage/infections # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/bigdragon/overlay_colors # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/catgirl/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/demon/alt_demon_appearances # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/gryphon/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/mantrap/eaten_mobs # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/overmap/stardog/weather_areas # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/pakkun/snapdragon/snappy/petters # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/peasant/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/raptor/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/scel/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/succubus/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/vampire/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/wolftaur/skins # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/simple_mob/vore/zorgoia/goia_overlays # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/temp_language_sources # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/temp_languages # mob: 15 mobs at boot; per-instance state, see audit
/mob/living/trait_injection_reagents # mob: 15 mobs at boot; per-instance state, see audit
/mob/logging # mob: 15 mobs at boot; per-instance state, see audit
/mob/misc_tabs # mob: 15 mobs at boot; per-instance state, see audit
/mob/mutations # mob: 15 mobs at boot; per-instance state, see audit
/mob/nutrition_messages # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/blob/blob_mobs # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/blob/has_langs # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/blob/resource_blobs # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/dead/exonet_messages # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/dead/visibleChunks # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/eye/aiEye/pic_in_pic/cameras_telegraphed # mob: 15 mobs at boot; per-instance state, see audit
/mob/observer/eye/visibleChunks # mob: 15 mobs at boot; per-instance state, see audit
/mob/proccall_handler/callers # mob: 15 mobs at boot; per-instance state, see audit
/mob/shouldnt_see # mob: 15 mobs at boot; per-instance state, see audit
/mob/speak_emote # mob: 15 mobs at boot; per-instance state, see audit
/mob/spell_list # mob: 15 mobs at boot; per-instance state, see audit
/mob/voice_sounds_list # mob: 15 mobs at boot; per-instance state, see audit
/mob/vore_fullness_ex # mob: 15 mobs at boot; per-instance state, see audit
/mob/vore_icon_bellies # mob: 15 mobs at boot; per-instance state, see audit
/mob/weight_messages # mob: 15 mobs at boot; per-instance state, see audit

# Baseline: rare types not yet converted.
/area/ambience # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/looking_glass/our_optional_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/looking_glass/our_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/redgate/stardog/eyes/our_eyes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/turret_controls # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/valid_flora # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/valid_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/area/valid_spawn_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/ref_holder/test_assoc_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/ref_holder/test_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/inventory/object_overlays # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/map_view_tg/viewing_clients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/movable/ability_master/ability_objects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/movable/pic_in_pic/shown_to # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/movable/pic_in_pic/viewing_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/atom/movable/screen/movable/spell_master/spell_objects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/completed_asset_jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/department_hours # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/misc_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/misc_tabs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/panel_tabs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/play_hours # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/screen_maps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/sent_assets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/client/spell_tabs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/accessory_slot_registry/slot_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/action/viewers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/inherent_laws # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/ion_laws # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/sorted_laws # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/state_inherent # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/state_ion # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/state_supplied # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ai_laws/supplied_laws # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alarm/sources # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alarm/sources_assoc # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alarm_handler/alarms # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alarm_handler/alarms_assoc # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alarm_handler/listeners # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/alternate_appearance/viewers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/candidates # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/cultist/harvested # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/cultist/sacrificed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/cultist/startwords # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/current_antagonists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/faction_members # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/global_objectives # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/pending_antagonists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/protected_jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/restricted_jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/roundstart_restricted # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/starting_locations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/antagonist/valid_species # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/artifact_effect/feelings/drastic_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/artifact_effect/feelings/messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/artifact_effect/forcefield/created_field # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/artifact_effect/gaia/my_glitterflies # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/artifact_effect/vampire/nearby_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/simple/icon_states/directions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/simple/namespaced/parents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet/sizes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet/sprites # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet/to_generate # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet_batched/entries # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet_batched/sizes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/asset/spritesheet_batched/sprites # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/autopsy_data_scanner/organs_scanned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/beam/elements # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/beam_components_cache/beam_components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/digest_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/digest_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/digest_modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/emote_lists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/examine_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/internal_contents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/items_preserved # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/slots # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/struggle_messages_inside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly/struggle_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/belly_overlay_tgui/state # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/benchmark/details # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/benchmark/metrics # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/benchmark/phases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/blob_type/ectoplasmic_horror/active_beams # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/checkers/current_board # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/checkers/possible_jumps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/checkers/selected_figure # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/checkers/valid_moves # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/chess/current_board # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/chess/last_double_pawn_move # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/chess/selected_figure # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/chess/valid_moves # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/four_row/placed_chips_pone # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/four_row/placed_chips_ptwo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/four_row/winning_tiles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/nine_mens/current_board # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/nine_mens/valid_moves # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/nine_mens/valid_removes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/rpg_dice/last_rolls # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/destroyed_ships_pone # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/destroyed_ships_ptwo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/ship_count_pone # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/ship_count_ptwo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/ships_placed_pone # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/ships_placed_ptwo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/shots_fired_pone # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/space_battle/shots_fired_ptwo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/vore_sweeper/placed_flags # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/vore_sweeper/placed_mines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/board_game/vore_sweeper/revealed_fields # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/category_item/underwear/tweaks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/camera/cameras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/ghost/hidden_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/obscured # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/obscuredTurfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/seenby # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/chunk/visibleTurfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/cinematic/locked # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/cinematic/watching # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/codex_tree/current_page # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/codex_tree/history # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/codex_tree/indexed_pages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/antag/changeling/absorbed_dna # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/antag/changeling/absorbed_languages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/antag/changeling/changeling_cooldowns # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/antag/changeling/purchased_powers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/antag/changeling/purchased_powers_history # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/bluespace_connection/connections # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/connect_range/turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/personal_crafting/categories # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/reactive_icon_update/triggering_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/recursive_move/parents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/schizophrenia/episode # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/shadekin/active_dark_maws # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/shadekin/shadekin_abilities # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/shadekin/shadekin_ability_datums # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/swarming/swarm_members # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/component/tourettes_disability/motor_tics # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/data/email_account/deleted # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/data/email_account/inbox # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/data/email_account/outbox # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/data/email_account/spam # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/program/chatclient/channel_history # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/program/ntnetdownload/downloads_queue # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/computer_file/program/nttransfer/connected_clients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/contract_definition/social/program/engineering_qualification/thresholds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/master/perf_outliers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/master/perf_tick_breakdown # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/master/perf_tick_realtime # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/master/perf_tick_usage # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/master/perf_worst_tick # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/rogue/all_zones # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/rogue/clean_zones # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/rogue/ready_zones # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/subsystem/air/pending_automatic_shutoff_valves # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/subsystem/dependencies # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/subsystem/mapping/multiz_levels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/subsystem/mapping/z_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/controller/subsystem/rolling_usage # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/crafting_recipe/blacklist # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/crafting_recipe/chem_catalysts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/crafting_recipe/parts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/crafting_recipe/reqs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/custom_item/req_titles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/messenger/conversations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/messenger/fakepdas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/messenger/tnote # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/nerdle/guesses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/nerdle/serialized_guesses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/notekeeper/storednotes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/pda/app/notekeeper/storedtitles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/data/record/fields # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/datacore/warrants # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/chemical_reaction/catalysts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/chemical_reaction/distilling/temp_range # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/chemical_reaction/inhibitors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/chemical_reaction/required_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/fusion_reaction/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/hierarchy/outfit/backpack_contents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/hierarchy/outfit/uniform_accessories # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/mob_organ_names/hit_zones # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/overmap_event_handler/hazard_by_turf # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/decl/overmap_event_handler/ship_events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/department/default_access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/department/jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/department/primary_jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/department/specialized_access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/design_techweb/category # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/design_techweb/materials # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dimension_theme/replace_floors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dimension_theme/replace_objs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/advance/symptoms # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/cures # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/required_organs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/roanoke/organ_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/strain_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/disease/viable_mobtypes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/SE # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/UI # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/body_markings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/custom_cold # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/custom_heat # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dna/species_traits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/dq_preview_flattener/runtime_paths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/footstep/steps_for_living # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/footstep_override/occupied_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/lootable/common_loot # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/lootable/rare_loot # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/lootable/uncommon_loot # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/element/lootable/unlucky_loot # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/embedded_program/memory # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/entity_narrate/entity_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/entity_narrate/entity_refs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/entity_narrate/tgui_selected_id_multi # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/error_viewer/error_cache/error_sources # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/error_viewer/error_cache/errors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/error_viewer/error_cache/errors_silenced # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/error_viewer/error_source/errors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/atmos_leak/excluded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/bluespace_locker/excluded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/brand_intelligence/infectedVendingMachines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/brand_intelligence/vendingMachines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/carp_migration/spawned_carp # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/economic_event/cheaper_goods # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/economic_event/dearer_goods # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/electrified_door/excluded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/gnat_migration/spawned_gnat # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/gravity/generators # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/grub_infestation/vents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/horde_infestation/alive_metroids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/horde_infestation/vents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/hostile_runtime/target_airlocks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/ionstorm/players # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/jellyfish_migration/spawned_jellyfish # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/metroid_infestation/alive_metroids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/metroid_infestation/vents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/prison_break/areaName # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/prison_break/areaNotType # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/prison_break/areaType # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/prison_break/areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/ray_migration/spawned_ray # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/rogue_drone/drones_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/shark_migration/spawned_shark # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/spacefish_migration/spawned_fish # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/spider_migration/spawned_spider # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event/supply_demand/required_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/airlock_failure/affected_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/blob/blobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/blob/excluded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/blob/open_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/brand_intelligence/infected_vending_machines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/brand_intelligence/vending_machines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/ghost_pod_spawner/desired_turf_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/ghost_pod_spawner/free_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/gravity/generators # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/infestation/things_to_spawn # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/infestation/turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/mob_spawning/spawned_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/prison_break/areas_to_break # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/event/window_break/collateral_windows # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/meta/departments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/meta/prison_break/irrelevant_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event2/meta/prison_break/relevant_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/event_container/last_event_time # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/eventkit/fake_pdaconvos/fakeJobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/eventkit/fake_pdaconvos/fakeRefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/eventkit/fake_pdaconvos/names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/events/events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/experiment/scanning/random/possible_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/experiment/scanning/required_atoms # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/experiment/scanning/scanned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/feed_channel/messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/feed_network/network_channels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/firemode/settings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/game_mode/antag_tags # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/game_mode/disabled_jobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/game_mode/latejoin_templates # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/gear/gear_tweaks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/geosample/find_presence # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/getrev/testmerge # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ghost_query/candidates # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ghost_query/check_bans # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ghosttrap/ban_checks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/hot_group/spot_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/hud/ammo_hud_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/hud/hud_elements # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/hud/minihuds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/identification/naming_lists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/industry/title_templates # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/industry/title_templates_ooc # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/industry/tokens # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/instrument/songs_using # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/job/access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/job/departments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/job/minimal_access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/job/restricted_keys # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/job/shift_keys # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/language/scramble_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/loadout_category/gear # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/locations/contents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/log_category/log_ring # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/log_holder/data_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/log_holder/verified_log_files # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lootpanel/contents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lootpanel/to_image # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/codex/category/children # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/codex/keywords # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/loremaster/organizations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/organization/added_ship_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/organization/destination_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/organization/flight_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/organization/ship_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/lore/organization/ship_prefixes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/magazine_appearance/state_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/magazine_appearance/thresholds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/map_report/bad_keys # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/map_report/bad_paths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/map_template/shelter/door_locations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material/window_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/composition # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/contributors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/cost_ledger # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/dissolved_gases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/feedstock_lots # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/field_treatments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/impurities # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/process_counts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/process_history # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/structure # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/surface_layers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/material_batch/test_results # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/metric/departments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/mind/objectives # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/mind/purchase_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/mind/special_verbs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/full_fear_down # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/full_fear_up # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/half_fear_down # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/half_fear_up # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/zero_fear_down # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/modifier/trait/phobia/zero_fear_up # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/moduletypes/modcount # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/money_account/transaction_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Parser/errors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Parser/tokens # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Parser/warnings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Scanner/errors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Scanner/nS_Scanner/delim # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Scanner/nS_Scanner/ignore # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_Scanner/warnings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_scriptOptions/nS_Options/keywords # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/n_scriptOptions/nS_Options/symbols # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/nifsoft/package/software # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/nifsoft/soulcatcher/brainmobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/BlockDefinition/functions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/BlockDefinition/initial_variables # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/BlockDefinition/statements # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/expression/FunctionCall/parameters # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/statement/FunctionCall/parameters # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/node/statement/FunctionDefinition/parameters # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/available_antag_software # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/available_news # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/available_station_software # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/banned_nids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/chat_channels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/email_accounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/fileservers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/registered_nids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet/relays # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet_conversation//clients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ntnet_conversation//messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/ore/xarch_ages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/parsed_map/gridSets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/parsed_map/grid_models # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/parsed_map/loaded_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/persistent_client/logging # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/persistent_client/played_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/persistent_client/player_actions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/persistent_client/post_login_callbacks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/persistent_client/post_logout_callbacks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/pipeline/leaks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/plane_holder/plane_masters # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/planet/expected_z_levels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/planet/planet_floors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/planet/planet_walls # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/planet/sun # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/planet_sunlight_handler/shandlers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/player_tips/HasReceived # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/cables # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/inputting # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/registered_source_refs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/registered_sources # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/registered_storage_demands # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/sleeping_apc_dynamic_loads # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/sleeping_apc_loads # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/sleeping_apc_power_classes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet/smes_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet_topology_job/components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet_topology_job/frontier # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet_topology_job/remaining # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/powernet_topology_job/target_nets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/predicate_compiler/errors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/predicate_compiler/watchable # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/preference_middleware/action_delegations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/preferences/middleware # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/preferences/randomise # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/preferences/recently_updated_keys # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/priority_queue/array # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/base_providers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/contributors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/defs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/interned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/measure_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/resolved_base # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/tag_bits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/property_registry/type_tables # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/choice_datums # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/choices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/choices_icons # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/choices_values # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/elements # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/radial_menu/page_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/automata/cave_system/ore_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/droppod/supplied_drop_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/map # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/maze/checked_coord_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/maze/closedlist # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/random_map/maze/openlist # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/reagent/drugs/high_message_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/reagent/drugs/sober_message_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/receptions/receiver_reception # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/robot_sprite/belly_capacity_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/robot_sprite/belly_light_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/robot_sprite/hat_offset # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/robot_sprite/sprite_animations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/robot_sprite/sprite_decals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/rogue/zonemaster/mineral_rocks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/rogue/zonemaster/mobspawns # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/rogue/zonemaster/rockspawns # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/rogue/zonemaster/spawned_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/emote_hear # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/emote_see # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_cannot # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_escalate # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_got_target # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_maybe_target # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_stand_down # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_threaten # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/say_understood # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/say_list/speak # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/seed/traits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/seed_pile/seeds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle/autodock/multi/destinations_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_autopath/path_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_destination/destinations_to_create # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_destination/routes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_destination/routes_to_make # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_web_master/autopaths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/shuttle_web_master/destinations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/signal/data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/song/allowed_instrument_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/song/channels_idle # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/song/channels_playing # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/sort_instance/runBases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/sort_instance/runLens # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/space_level/traits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/assisted_langs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/cold_discomfort_strings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/default_emotes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/env_traits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/food_preference # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/genders # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/has_limbs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/has_organ # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/heat_discomfort_strings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/inherent_verbs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/lleill/lleill_ability_datums # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/lleill/valid_transform_species # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/secondary_langs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/shapeshifter/hanner/lleill_ability_datums # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/shapeshifter/valid_transform_species # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/skin_overlays # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/species_component # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/species_language # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/speech_chance # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/speech_sounds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/traits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/species/unarmed_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/aoe_turf/conjure/newVars # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/aoe_turf/conjure/summon_type # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/level_max # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/spell_levels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/targeted/compatible_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/targeted/equip_item/equipped_summons # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/targeted/equip_item/summoned_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/spell/targeted/genetic/mutations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/sprite_accessory/hide_body_parts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/sprite_accessory/species_allowed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/sprite_accessory/tail/lower_layer_dirs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stack/contents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/status_effect/grouped/sources # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/articles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/borrow_brokers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/borrows # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/shareholders # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stock/values # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stockMarket/balances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stockMarket/last_read # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stockMarket/logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stockMarket/stockBrokers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/stockMarket/stocks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/supply_pack/contains # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/supply_pack/manifest # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/symptom/bodies # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/symptom/prefixes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/symptom/suffixes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/symptom/viralincubate/captives # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/talking_atom/heard_words # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/available_experiments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/available_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/boosted_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/completed_experiments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/consoles_accessing # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/custom_designs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/deconstructed_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/deferred_tier_roots # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/discovered_mutations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/hidden_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/last_bitcoins # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/research_logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/research_points # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/research_queue_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/researched_designs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/researched_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/skipped_experiment_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/techweb_servers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/tiers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb/visible_nodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb_node/design_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb_node/prereq_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/techweb_node/research_costs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgs_api/v5/chunked_topics # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgs_api/v5/pending_events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui/children # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_earstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_facial_hairstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_gradstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_hairstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_species # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_tailstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/appearance_changer/valid_wingstyles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/atmos_control/monitored_alarms # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/camera/additional_networks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/camera/concurrent_users # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/camera/network # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/communications/req_access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_module/ship/fullmonty/known_sectors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tgui_window/oversized_payloads # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tickets/active_tickets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tickets/closed_tickets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/tickets/resolved_tickets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trackable/cameras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trackable/humans # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trackable/namecounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trackable/names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trackable/others # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trade_destination/temp_price_change # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trade_destination/viable_random_events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trait/neutral/food_pref/our_allergens # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/trait/primitive_expression_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transcore_db/backed_up # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transcore_db/body_scans # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transcore_db/has_left # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transcore_db/implants # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transhuman/body_record/limb_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transhuman/body_record/organ_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/transhuman/mind_record/nif_savedata # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/turbolift/doors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/turbolift/floors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/turbolift/music # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/turbolift/queued_floors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/turbolift_floor/doors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/unit_test/chemical_reactions_shall_not_conflict/result_reactions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/unit_test/template_noops/log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/unit_test/wire_test/cable_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/unit_test/wire_test/dirs_checked # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/unit_test/wire_test/exempt_from_wires # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/uplink_item/crated/paths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/visualnet/camera/cameras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/visualnet/chunks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vore_preferences/belly_prefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vore_preferences/nutrition_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vore_preferences/soulcatcher_prefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vore_preferences/weight_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vote/choices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/vote/voted # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/weather/transition_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/weather_holder/allowed_weather_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/weather_holder/forecast # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/weather_holder/roundstart_weather_chances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/flags # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/gases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/molar_mass # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/molar_specific_volume # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/name # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/overlay_limit # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/overlays # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/specific_heat # rare: under ~50 instances at boot, not converted yet (audit baseline)
/datum/xgm_gas_data/tile_overlay # rare: under ~50 instances at boot, not converted yet (audit baseline)
/image/client_only/clients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorb_chance_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorb_chance_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorb_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorb_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorbed_struggle_messages_inside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/absorbed_struggle_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/autotransfer_queue # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/autotransferextralocation # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/autotransferextralocation_secondary # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/belly_surrounding # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/digest_chance_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/digest_chance_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/digest_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/digest_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/emote_lists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_absorbed_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_absorbed_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_absorbed_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_attempt_absorbed_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_attempt_absorbed_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_attempt_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_attempt_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_fail_absorbed_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_fail_absorbed_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_fail_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_fail_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_item_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_item_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_item_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/escape_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/examine_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/examine_messages_absorbed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/fullness1_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/fullness2_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/fullness3_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/fullness4_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/fullness5_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/generated_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/items_preserved # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/primary_autotransfer_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/primary_autotransfer_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/primary_transfer_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/primary_transfer_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/secondary_autotransfer_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/secondary_autotransfer_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/secondary_transfer_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/secondary_transfer_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/struggle_messages_inside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/struggle_messages_outside # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/trash_eater_in # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/trash_eater_out # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/unabsorb_messages_owner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/belly/unabsorb_messages_prey # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/anomaly/dimensional/target_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/anomaly/weather/affected_areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/anomaly/weather/affected_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/bmode/buildholder/selected_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/blood/drip/drips # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/blood/tracks/stack # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/blood/viruses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/mucus/viruses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/random_icon_states # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/cleanable/vomit/viruses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/mecha_wreckage/crowbar_salvage # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/decal/mecha_wreckage/welder_salvage # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/env_message/message_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/fake_sun/possible_light_setups # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/fusion_em_field/dormant_reactant_quantities # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/fusion_em_field/particle_catchers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/gibspawner/gibamounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/gibspawner/gibdirections # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/gibspawner/gibtypes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/map_effect/beam_point/my_beams # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/map_effect/interval/sound_emitter/sounds_to_play # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/map_effect/portal/master/portal_lines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/generic_waypoints # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/map_z # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/possible_descriptors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/restricted_waypoints # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/ship/engines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/overmap/visitable/ship/speed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/plant/neighbors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/rune/converting # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/shut_controller/parts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/spider/spiderling/grow_as # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/step_trigger/thrower/affecting # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/effect/temporary_effect/pulse/snake/iterated_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ammo_magazine/ammo_states # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ammo_magazine/cell_mag/modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ammo_magazine/icon_keys # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ammo_magazine/stored_ammo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/areaeditor/areaColor_turfs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/autopsy_scanner/chemtraces # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/autopsy_scanner/wdata # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/backup_implanter/imps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/bedsheet/dream_messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/book/bundle/pages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/book/tome/words # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/bug_monitor/cameras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/camera/picture_planes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/camera/siliconcam/aipictures # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card/files # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card/id/access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card/id/event/title_strings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card/initial_sprite_stack # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card_fluff/initial_sprite_stack # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/card_fluff/sprite_stack # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/cartridge/messenger_plugins # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/cartridge/programs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/cartridge/stored_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/mask/chewable/filling # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/mask/gas/filtered_gases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/mask/gas/poltergeist/heard_talk # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/mask/paper/papermask_designs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/shoes/clown_shoes/squeak_sound # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/shoes/inside_emotes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/clothing/shoes/mech_shoes/squeak_sound # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/commcard/active_devices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/commcard/internal_data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/commcard/internal_devices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/commcard/ui_templates # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/communicating # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/im_contacts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/im_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/known_devices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/modules # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/voice_invites # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/voice_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/communicator/voice_requests # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/computer_hardware/hard_drive//stored_files # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/deck/cards # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/depth_scanner/positive_locations # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/detective_scanner/stored # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/disk/botany/genes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/disk/design_disk/blueprints # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/disk/transcore/stored # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dnalockingchip/stored_dna # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/deliverylists # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/deliveryslot_1 # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/deliveryslot_2 # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/deliveryslot_3 # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/injection_chems # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/sleeper/items_preserved # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/dogborg/stasis_clamp/clamps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/electronic_assembly/component_positions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/encryptionkey//channels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/extrapolator/extracted_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/floor_painter/decals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/fuel_assembly/rod_quantities # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/glass_jar/accept_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/grenade/chem_grenade/allowed_containers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/grenade/chem_grenade/beakers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/grenade/shooter/projectile_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gripper/can_hold # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gripper/pockets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/burst_accuracy # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/dispersion # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/energy/kinetic_accelerator/modkits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/energy/modular/guncomponents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/firemodes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/launcher/grenade/grenades # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/launcher/rocket/rockets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/launcher/syringe/darts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/projectile/dartgun/beakers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/projectile/dartgun/mixing # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/projectile/loaded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/projectile/revolver/lemat/secondary_loaded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/gun/projectile/revolver/lemat/tertiary_loaded # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/hand/cards # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/holosign_creator/signs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/language/languages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/organ/limbaugment/possible_targets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/reagent_generator/emote_descriptor # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/reagent_generator/empty_message # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/reagent_generator/generated_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/reagent_generator/random_emote # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/implant/reagent_generator/self_emote_descriptor # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/activators # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/input/microphone/sign/my_langs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/input/video_camera_input/paired_cameras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/inputs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/inputs_default # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/output/sound/sounds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/outputs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/outputs_default # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/integrated_circuit/passive/power/chemical_cell/fuel # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/kit/allowed_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/lasertagknife/allowed_suits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/mail/stamps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/mapping_unit/icon_image_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/mapping_unit/map_image_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/material/fishing_net/accepted_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/mecha_parts/component/armor/damage_absorption # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/megaphone/insultmsg # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/megaphone/super/color_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/megaphone/super/font_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/megaphone/super/volume_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/melee/artifact_blade/abilities # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/melee/cursedblade/voice_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/modular_computer/idle_threads # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/multitool/modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/newspaper/news_content # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/nif/nifsofts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/nif/nifsofts_life # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/nif/planes_visible # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/nif/starting_software # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ore_bag/stored_ore # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/ore_chunk/stored_ore # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/organ/internal/fruitgland/poison_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pack//cards # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/paper/ico # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/paper/offset_x # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/paper/offset_y # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/paper_bin/papers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/paper_bundle/pages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pda/multicaster/cartridges_to_send_to # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pda/notifying_programs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pda/programs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pda/shortcut_cache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pda/shortcut_cat_order # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/perfect_tele/beacons # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/perfect_tele/logged_events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/perfect_tele/radial_images # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/perfect_tele/warned_users # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/pizzabox/boxes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/arc/fragmentation/fragment_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/arc/vial/my_chems # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/beam/chain_lightning/hit_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/beam/lasertag/allowed_suits # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/energy/blob/my_chems # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/impacted_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/permutated # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/spell_projectile/trails # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/submunitions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/projectile/test/hit # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/radio/channels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/radio/headset/event/remove_spells # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/radio/headset/event/spells # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rcd/effects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rcd/modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/borghypo/reagent_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/borghypo/reagent_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/borghypo/reagent_volumes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/borghypo/saved_recipes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/borghypo/transfer_amounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/cooking_container/insertable # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/drinks/glass2/extras # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/csandwich/ingredients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/customizable/ingredients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/donkpocket/heated_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/nutriment_desc # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/rhubarbcustard/color_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/food/snacks/saucer/color_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/glass/beaker/vial/culture/data # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/glass/beaker/vial/culture/diseases # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/glass/beaker/vial/random/random_reagent_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/reagent_containers/hypospray/filled_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rectape/storedinfo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rectape/timestamp # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/research_sample/resource_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/retail_scanner/item_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/retail_scanner/price_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/retail_scanner/transaction_logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rig/installed_modules # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rig_module/stat_modules # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rig_module/vision/vision_modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/rms/modes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/added_networks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/emag # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/modules # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/original_languages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/subsystems # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/supported_upgrades # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robot_module/synths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/robotic_multibelt/cyborg_integrated_tools # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/sample/evidence # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/selectable_item/item_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/self_repair_system/target_components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/shield_projector/active_shields # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/slime_grinder/to_be_processed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/spell/control/controlled_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/spell/energy_siphon/things_to_siphon # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/spell/summon/summon_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/stack/medical/splint/splintable_organs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/storage/box/fluff/has_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/survivalcapsule/superpose/template_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/t_scanner/active_scanned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/target/bulletholes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/taskmanager/scanned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_catalog/assistance_instances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_catalog/consumable_instances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_catalog/equipment_instances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_catalog/spell_instances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_core/appearances # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_core/spells # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_core/summoned_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/technomancer_core/wards_in_use # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/tool/transforming/possible_tooltypes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/toy/eight_ball/possible_answers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/tray/carrying # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/universal_translator/limited/known_languages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/uv_light/reset_objects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/uv_light/scanned # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/uv_light/stored_alpha # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/vac_attachment/vac_settings # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/vampiric/nearby_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/item/vampiric/shadow_wights # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/abstract_grub_machine/active_power_usages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/abstract_grub_machine/idle_power_usages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/appliance/cooking_objs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/appliance/output_options # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/binary/algae_farm/storage_capacity # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/binary/algae_farm/stored_material # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/omni/atmos_filter/atmos_filters # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/omni/atmos_filter/filtering_outputs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/omni/mixer/inputs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/omni/mixer/mixing_inputs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/omni/ports # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/atmospherics/trinary/atmos_filter/filtered_out # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/autolathe/imported_designs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/beehive/frames # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/feedback # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/messages # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/messages_admin # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_cargo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_command # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_common # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_deathsquad # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_engineering # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_explorer # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_medical # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_raider # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_science # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_security # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_service # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/blackbox_recorder/msg_syndicate # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/camera/network # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/cash_register/item_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/cash_register/price_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/cash_register/transaction_logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/casino_prize_dispenser/log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_analyzer/found_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_dispenser/cartridges # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_dispenser/dispense_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_dispenser/saved_recipes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/cartridges # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/catalyst_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/dispense_reagents # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/queue # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/recipes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/chemical_synthesizer/spawn_cartridges # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/clonepod/containers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/HolodeckControl/holographic_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/HolodeckControl/holographic_objs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/HolodeckControl/supported_programs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/orion_trail/events # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/orion_trail/settlers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/orion_trail/stopblurbs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/orion_trail/stops # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/prizes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/arcade/special_prizes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/area_atmos/connectedscrubbers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/cryopod/_admin_logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/cryopod/frozen_crew # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/cryopod/frozen_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/fusion_core_control/connected_devices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/general_air_control/sensor_information # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/general_air_control/sensors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/guestpass/accesses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/guestpass/internal_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/mecha/located # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/scan_consolenew/buffers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/security/network # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/ship/helm/known_sectors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/shuttle/authorized # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/shuttle_control/emergency/authorized # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/supplycomp/expanded_packs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/telecomms/monitor/machinelist # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/telecomms/server/servers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/telecomms/traffic/servers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/telecomms/traffic/viewingcode # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/computer/telescience/crystals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/contraband_scanner/contraband # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/door/blast/puzzle/locks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/door/blast/puzzle/tyrdoor/keypad/code # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/door/blast/puzzle/tyrdoor/keypad/lastattempt # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/door_timer/targets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/doppler_array/detected_explosions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/embedded_controller/radio/airlock/dummy_terminals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/embedded_controller/radio/docking_port_multi/child_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/embedded_controller/valid_actions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/exonet_node/logs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/floorlayer/mode # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/food_replicator/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/gear_dispenser/dispenses # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/gear_painter/allowed_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/gravity_generator/main/areas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/gravity_generator/main/levels # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/gravity_generator/main/parts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/hyperpad/centre/linked # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/icecream_vat/product_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/implantchair/implant_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/injector_maker/beaker_reagents_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/librarycomp/checkouts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/librarycomp/inventory # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/magnetic_controller/magnets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/magnetic_controller/rpath # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/maint_recycler/granted_points # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/maint_recycler/hostile_towards # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/maint_vendor/product_datums # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mecha_part_fabricator_tg/queue # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mecha_part_fabricator_tg/queue_producer_accounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/media/jukebox/ghost/custom_tracks # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/message_server/pda_msgs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/message_server/rc_msgs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/message_server/spamfilter # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/meter/pipes_on_turf # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mineral/processing_unit/ores_processing # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mineral/processing_unit/ores_stored # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mineral/stacking_machine/stack_paths # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mineral/stacking_machine/stack_storage # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mining/drill/gas_field # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mining/drill/ore_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mining/drill/resource_field # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mining/drill/stored_ore # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/mining/drill/supports # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/navbeacon/codes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/ntnet_relay/dos_sources # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/nuclearbomb/wires_list # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/ore_silo/holds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/ore_silo/ore_connected_machines # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/organ_printer/anomalous_products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/organ_printer/complex_products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/organ_printer/engineered_products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/organ_printer/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/partslathe/materials # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/partslathe/queue # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/partslathe/queue_producer_accounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/partslathe/storage_capacity # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/pda_multicaster/internal_PDAs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/petrification/remotes # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/pointdefense_control/targets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/porta_turret/lasertag/vests_to_target # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/portable_atmospherics/powered/scrubber/scrubbing_gas # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/hydromagnetic_trap/active_field # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/hydromagnetic_trap/fields_in_range # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/hydromagnetic_trap/things_in_range # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/sensor/history # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/shield_generator/damaged_segments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/shield_generator/field_segments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/power/smes/batteryrack/internal_cells # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/processor/to_be_processed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/reagentgrinder/holdingitems # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/recycling/sorter/materials # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/clothing/created_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/clothing/tgui_vore_selection # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/clothing/viable_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/construction # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/spawning_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/stored_materials # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/tgui_construction # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/vore/created_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/vore/tgui_vore_selection # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/replicator/vore/viable_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/requests_console/message_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/seed_storage/contraband_seeds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/seed_storage/piles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/seed_storage/piles_contra # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/seed_storage/scanner # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/seed_storage/starting_seeds # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/shield_gen/capacitors # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/shield_gen/field # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/shieldgen/deployed_shields # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/shieldgen/regenerating # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/sleeper/available_chemicals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/sleeper/base_chemicals # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/smartfridge/item_records # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/suit_cycler/limit_departments # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/allinone/linked_radios_weakrefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/autolinkers # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/broadcaster/linked_radios_weakrefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/freq_listening # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/links # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/receiver/linked_radios_weakrefs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/server/TrafficActions # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/server/log_entries # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/server/memory # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/telecomms/server/stored_names # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/transhuman/synthprinter/stored_material # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/contraband # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/premium # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/prices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/product_records # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/vending/refill # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/machinery/washing_machine/washing # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/cargo # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/combat/phazon/inherent_damage_absorption # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/hull_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/internal_components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/micro_utility_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/micro_weapon_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/operation_req_access # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/proc_res # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/special_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/starting_components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/universal_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/utility_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/mecha/weapon_equipment # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/random/mob/semirandom_mob_spawner/possible_mob_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/singularity/energy_ball/orbiting_balls # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/soulgem/brainmobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/bedsheetbin/sheets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/blob/factory/spores # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/boxpile/ckeys_that_took # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/candybowl/candy # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/candybowl/treated # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/loot/code # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/loot/lastattempt # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/lootsafe/devillock/code # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/lootsafe/devillock/lastattempt # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/lootsafe/numberlock/code # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/closet/crate/secure/lootsafe/numberlock/lastattempt # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/coatrack/allowed # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/event/santa_sack/ckey_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/event/santa_sack/nice_list_log # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/event_collector/possible_ingredients # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/event_collector_blocker/active_repair_steps # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/flora/tree/pine/xmas/presents/ckeys_that_took # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/frame/components # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/gargoyle/tail_lower_dirs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/janitorialcart/tgui_icons # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/loot_pile/icon_states_to_use # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/loot_pile/searchedby # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/medical_stand/transfer_amounts # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/mob_spawner/scanner/mobs_in_range # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/mob_spawner/spawn_types # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/mob_spawner/spawned_mobs # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/morgue/crematorium/vr/allowed_items # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/morgue/occupants # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/musician/allowed_instrument_ids # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/ore_box/stored_ore # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/picnic_blanket_deployed/attached_blankets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/prop/dark_node/children_effects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/prop/lock/linked_objects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/prop/prismcontrol/my_turrets # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/reflector/allowed_projectile_typecache # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/reflector/has_projectiles # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/toilet/currently_held_objects # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/structure/trash_pile/searchedby # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/bank # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/interact_sound # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/multiple # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/prices # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/trader/start_products # rare: under ~50 instances at boot, not converted yet (audit baseline)
/obj/turbolift_map_holder/areas_to_use # rare: under ~50 instances at boot, not converted yet (audit baseline)
/turf/snow/crossed_dirs # rare: under ~50 instances at boot, not converted yet (audit baseline)
