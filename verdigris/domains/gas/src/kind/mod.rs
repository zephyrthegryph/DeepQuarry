//! Components bound to DM atoms through the entity table
//! (`doc/rewrite/rust_bindings.md`), as opposed to `vg-gas`'s turf/pipe
//! grid kinds. The pump is the reference component (§14).

pub mod pump;
