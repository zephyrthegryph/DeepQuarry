[env]
# Auxmos integration tests exercise process-global arenas and generated BYOND
# init hooks. They are intentionally serialized; parallel libtest workers can
# reinitialize another test's arena and create false failures.
RUST_TEST_THREADS = "1"
